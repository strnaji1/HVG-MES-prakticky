# Spuštění aplikace
# Otevři terminál ve složce projektu
# Vytvoř virtuální prostředí:
# python -m venv venv
aktivace prostředí:
# Windows:
# venv\Scripts\activate
# Linux / macOS:
# source venv/bin/activate
# Nainstaluj závislosti:
# pip install -r requirements.txt
# Spusť aplikaci:
# run app.py --server.fileWatcherType poll --server.port 8052

Aplikace na adrese: http://localhost:8052
